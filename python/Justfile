python := if os() == "windows" { "python" } else { "python3" }

default: check

check:
    {{python}} scripts/check.py check

format:
    {{python}} scripts/check.py format

sync:
    {{python}} scripts/check.py sync

lock:
    {{python}} scripts/check.py lock

build:
    {{python}} scripts/check.py build

package-check:
    {{python}} scripts/check.py package-check

dependency-audit:
    {{python}} scripts/check.py dependency-audit

workflow-check:
    {{python}} scripts/check.py workflow-check
